void main() {
  // String myString = 'abc';

  //  print length
  // print(myString.length);
  // //
  // print(myString[0]);

  // myString.contains('a');
  // myString.contains('x');

  //split() subString()
  // String reverse = '';

  // for (int i = myString.length - 1; i >= 0; i--) {
  //   reverse = reverse + myString[i];
  // }

  // print(reverse);
  // for (int i = myString.length - 1; i >= 0; i--) {
  //   print(myString[i]);
  // }
}
