void main() {
  // ===========================================================================
  // Maps
  // ===========================================================================
  Map<int, String> mapOfMonths = {
    1: 'Jan',
    2: 'Feb',
    3: 'Mar',
    4: 'Apr',
    5: 'May',
    6: 'Jun',
    7: 'Jul',
    8: 'Aug',
    9: 'Sep',
    10: 'Oct',
    11: 'Nov',
    12: 'Dec',
  };

  print(mapOfMonths);

  // List<String> listOfMonths = mapOfMonths.values.toList();
  // print(listOfMonths);

  // mapOfMonths.clear();

  // List<int> listOfKeys = mapOfMonths.keys.toList();

  // if (mapOfMonths.containsKey(4)) {
  //   print(mapOfMonths[4]);
  // } else {
  //   print('not available');
  // }

  // mapOfMonths.forEach((key, value) {
  //   print('Key is $key and value is $value');
  // });

  // mapOfMonths.addAll({13: 'month'});

  // mapOfMonths.update(1, (value) => 'January');

  //print(mapOfMonths);
}
